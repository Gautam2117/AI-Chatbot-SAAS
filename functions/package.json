{
  "name": "functions",
  "description": "Cloud Functions for Firebase (OTP + Blocking + SMTP)",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"Skipping lint (no ESLint configured)\""
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1",
    "nodemailer": "^6.9.13"
  },
  "private": true
}
